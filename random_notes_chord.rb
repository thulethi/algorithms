require 'rspec'

# Write a function which given a sequence of cords, e.g “d, G, CMaj, CMaj” generates a random melody
# from notes of these chords, e.g. outputs “d f a, g d b, c e g, e g c”.
# Let’s suppose it generates 3 notes per chord. Hint: ask for help.

RSpec::Core::Runner.run([$__FILE__])
